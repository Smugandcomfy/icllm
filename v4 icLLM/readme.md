The v4 WASM patched graph_max_nodes. 
469mb models to 1.1gb models
 --ctx-size 4096, --cache-type-k q8_0, --cache-type-v q8_0, --batch-size 256, --ubatch-size 128, --flash-attn on                    
the ICP instruction limit is the real bottleneck — 0.8B+ models are too slow to run.



